# Session ID
`SHO-20260407-0001`

# Generated At
`2026-04-07T09:32:45Z`

# Offload Reason
当前窗口已完成一次关键问题诊断：post-B5 的剩余问题不再属于 `PLAN-20260405-003` 的主线重构收尾，而是一个新的架构纠偏主题。核心问题不是“有没有 preflight”，而是 room-start contract semantics 的 owner 被拆裂到了 planner 输出、`preflight.py` 规则分类器、以及 harness 注入的 validation contract。为避免继续在同一窗口/同一计划里混叙事，需要把后续工作切换到新的纠偏计划和新的窗口。

# Session Goal
为下一窗口提供一个可直接续接的上下文包，使其能够基于新的子计划 `PLAN-20260407-004` 继续处理 room-start contract owner correction，而不用重新推导本窗口已经完成的架构判断、代码漂移诊断和计划治理切换。

# Overview
- `PLAN-20260405-003` 的主线重构目标已完成：P1 会议主线已经切换到 `pre-room planning + Host-led orchestration + dynamic specialists + synthesis + control gate`
- 当前暴露出来的新问题不是旧主线未完成，而是 post-B5 修复手段本身产生了新的 owner drift：
  - planner/open questions 在定义一部分 room-start 语义
  - `src/decision_room/orchestration/preflight.py` 用 taxonomy/heuristics 在定义另一部分 room-start 语义
  - smoke/evaluation harness 又注入了一套 validation contract
- 诊断结论已经收敛：
  - `preflight` 不是层
  - `preflight` 也不应成为第二个模块 owner
  - 正确 owner 只能是 agent-owned `Orchestration` 的会前 contract-assessment capability
  - `Runtime/Control` 只该保留 declarative contract plumbing、machine-known facts 注入、gate 执行与持久化
  - validation-specific 语义应留在 evaluation/test harness，不应继续外溢到 product surface
- 已新建纠偏子计划 `PLAN-20260407-004`
- 已把 `PLAN-20260405-003` 的后续指针改为转入新计划，而不是继续在原计划内扩张 amendment

# Related Refs
- `docs/plans/active/PLAN-20260407-004.md`
- `docs/plans/active/PLAN-20260405-003.md`
- `docs/plans/PLAN_INDEX.json`
- `docs/tech/MAS-harness-architecture-baseline.md`
- `docs/tech/P1-host-led-meeting-topology-baseline.md`
- `docs/tech/P1-blocked-open-question-rca-20260406.md`
- `docs/prd/Decision-Room-PRD-v0.4.md`
- `src/decision_room/orchestration/preflight.py`
- `src/decision_room/runtime/room_runtime.py`
- `src/decision_room/orchestration/room_executor.py`
- `src/decision_room/orchestration/operator_entry_contracts.py`
- `scripts/smoke_real_room_path.py`
- `frontend/src/routes/home.tsx`
- `frontend/src/routes/room.tsx`
- `frontend/src/routes/results.tsx`

# Subtask Snapshots
## 1. 计划治理已切换
- 新计划：`PLAN-20260407-004`
- 父计划：`PLAN-20260405-003`
- 当前判断：`PLAN-20260405-003` 已不再适合作为 post-B5 纠偏执行入口，因为它的主任务已经完成；继续在它里面追加 amendment 只会模糊问题边界

## 2. 高置信诊断结论
- 问题不在 “是否需要 room-start contract assessment”
- 问题在 “room-start contract semantics 的 owner 被拆裂”
- 正确边界：
  - `Orchestration`：owner
  - `Runtime/Control`：plumbing + gate
  - `Governance/Harness`：evaluation/smoke acceptance
  - `Product Surface`：只消费通用 room-start contract，不产品化 validation panels

## 3. 当前最值得警惕的代码点
- `src/decision_room/orchestration/preflight.py`
  - taxonomy/heuristics 已经接近第二个语义 owner
- `src/decision_room/runtime/room_runtime.py`
  - `planning -> rule adjudicator -> room_start_ready gate` 的 sequencing 强化了双 owner 结构
- `src/decision_room/orchestration/room_executor.py`
  - prompt 仍直接消费多项 validation-specific contract
- `frontend/src/routes/home.tsx`
  - 把 preflight 作为创建会议的中心心智
- `frontend/src/routes/room.tsx`
- `frontend/src/routes/results.tsx`
  - 仍直接展示 `validation_scenario / projection_contract / evidence_contract / conclusion_contract`

## 4. 当前计划入口
- 从 `PLAN-20260407-004` 的 `C1` 开始
- 第一件事不是继续写更多 preflight 规则，也不是先改 UI
- 第一件事应是先写清 owner 切分表：
  - 哪些语义判断必须回收到 planner/agent 输出
  - 哪些只保留为 runtime/control 的声明式 gate
  - 哪些从 product surface 删除，只留在 evaluation harness

# Avoid Repeat
- 不要把这次问题重新表述成“要不要有 preflight”；真正的问题是 owner 与边界，而不是功能名词。
- 不要继续往 `preflight.py` 增加 taxonomy/heuristics；这会进一步把规则代码写成第二个语义 owner。
- 不要继续把 validation-specific acceptance contract 留在产品页面上展示；这会把 harness 语义再次产品化。

# Next Window Boot
1. 先读 `Offload Reason` 和 `Overview`
2. 再读 `Subtask Snapshots`
3. 再读新计划：
   - `docs/plans/active/PLAN-20260407-004.md`
4. 再读三份关键诊断/基线：
   - `docs/tech/MAS-harness-architecture-baseline.md`
   - `docs/tech/P1-host-led-meeting-topology-baseline.md`
   - `docs/tech/P1-blocked-open-question-rca-20260406.md`
5. 再查看当前热点代码：
   - `src/decision_room/orchestration/preflight.py`
   - `src/decision_room/runtime/room_runtime.py`
   - `src/decision_room/orchestration/room_executor.py`
   - `frontend/src/routes/home.tsx`
   - `frontend/src/routes/room.tsx`
   - `frontend/src/routes/results.tsx`
6. 最后再开始新的 planning/execution

Before coding:
- 先按 `PLAN-20260407-004` 的 `C1` 产出 owner 切分表；不要直接开始删字段或重命名 `preflight`，否则很容易再次把实现名词当成架构问题本身。

# Source of Truth
- 新执行入口：
  - `docs/plans/active/PLAN-20260407-004.md`
- 当前计划治理真相：
  - `docs/plans/PLAN_INDEX.json`
  - `docs/plans/active/PLAN-20260405-003.md`
  - `docs/plans/active/PLAN-20260407-004.md`
- 架构真相：
  - `docs/tech/MAS-harness-architecture-baseline.md`
  - `docs/tech/P1-host-led-meeting-topology-baseline.md`
- 诊断真相：
  - `docs/tech/P1-blocked-open-question-rca-20260406.md`
  - `src/decision_room/orchestration/preflight.py`
  - `src/decision_room/runtime/room_runtime.py`
  - `src/decision_room/orchestration/room_executor.py`
- 产品入口真相：
  - `docs/prd/Decision-Room-PRD-v0.4.md`
  - `frontend/src/routes/home.tsx`
  - `frontend/src/routes/room.tsx`
  - `frontend/src/routes/results.tsx`
