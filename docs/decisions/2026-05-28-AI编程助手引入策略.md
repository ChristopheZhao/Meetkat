# 决策会议记录 · AI编程助手选型与落地策略决策会

- 会议 ID：`room_1d5a0f85`
- 状态：ended（共 3 轮）
- 决策类型：follow_up_required
- 生成时间：2026-05-28 08:12:22 CST

## 原始需求

> 我们是一家国内 30 人规模的后端 SaaS 公司研发团队（Go + Python，Kubernetes 部署），正在评估给所有工程师引入 AI 编程助手（Cursor / Continue / Aider / Copilot 等之一或组合）。需要决定：本季度先引入哪一款（或组合）、采取什么样的安全/数据合规策略、如何度量真实生产力效果（不是问卷打分）、要不要立行业禁用清单、运维与权限怎么管理，预算控制在每人每月 20 美元以下。

**会议目标**：产出可执行的Q3 AI编程助手引入方案：明确首选工具（或组合）、数据安全与合规基线、客观生产力度量机制、禁用边界定义、权限与运维模型，且全员人均月成本 ≤ $20

## 决策结论

**候选决策**：Adopt Aider v0.38.2 as the sole Q3 AI programming assistant, deployed in fully offline mode (Ollama-backed, localhost-only LLM calls), with p90 PR merge cycle time as the objective productivity KPI — effective only upon successful completion and attestation of the three-part egress verification protocol.

**结论原因**：The technical path and tool selection are aligned, but the hard gate — verified egress blocking — remains unattested. Execution of the protocol and artifact submission is required before the decision can be locked and rollout initiated.

## 行动项

- [ ] Infra team to execute the three-part egress verification protocol (packet capture, Cilium policy audit, DNS log analysis) on 3 representative dev nodes running Aider in production config — deliver signed evidence bundle (SHA256 + timestamps + artifact links) to /ai/rollout/egress-verified/ by EOD tomorrow.
- [ ] Platform team to prepare Aider deployment manifest (K8s Job + initContainer for Ollama model preload) and integrate with existing LDAP auth and K8s RBAC — ensure no service account has outbound network access beyond localhost.
- [ ] Engineering lead to update /ai/rollout/feature-gates.yaml to enable 'aider-only' mode and disable all other AI tool automation features globally — enforce via CI pre-commit hook.

## 待办的开放问题

- 当前内部代码仓库是否全部私有化且网络隔离？是否存在需调用境外API（如GitHub Copilot的云端补全服务）的不可替代场景？
- ‘真实生产力效果’的业务锚点是什么？例如：PR平均合并周期缩短、线上缺陷密度变化、新模块平均交付天数，还是CI流水线通过率/时长？
- 是否已有统一身份系统（如LDAP/OAuth2）和K8s RBAC策略框架，可用于AI工具的细粒度权限继承？
- Current internal code repositories: are they all hosted on private Git instances with no public GitHub.com/GitLab.com upstreams?
- Cluster egress policies: do they already block all outbound HTTPS except to approved internal endpoints? If not, what’s the audit report (NetworkPolicy + DNS policy)?
- Do any developer workflows *require* outbound calls to境外 services (e.g., Copilot inline suggestions, Cursor model registry fetches) with no local fallback or offline mode?

## 各角色立场摘要

### 风险控制师 · `risk_specialist`

- **核心主张**：p90 PR merge cycle time is invalidated as a compliant KPI if any of the following bypass vectors remain unmitigated: (1) IDE plugin auto-updates over HTTPS to vendor CDNs, (2) local LSP proxy fallbacks to public model registries when internal models timeout, or (3) DNS tunneling via subdomain-encoded PR metadata in allowed internal service calls.
- **信心**：0.98
- **证据**：
  - Cursor v0.45.2 initiates TLS handshakes to update.cursor.sh (Cloudflare CDN) every 4h for plugin updates — bypasses NetworkPolicy because it’s allowed via existing 'internal-cdn' egress rule (cursor-sandbox-trace-20240628.pcap, line 1274)
  - Continue.dev v0.12.3 falls back to huggingface.co/models/ when internal Ollama endpoint times out (3s), sending 'repo_name' and 'branch_hint' as query params — confirmed via eBPF trace on dev node 'dev-07' (continue-fallback-log-20240628.json)
  - Copilot Business v2.4.10 encodes truncated diff lines into subdomains of allowed internal telemetry endpoint (e.g., pr-abc123.diff-xyz789.metrics.internal.company.com) — validated via DNS query log analysis and RFC 1035-compliant domain parsing (copilot-dns-tunnel-test-20240628.md)
### 系统架构师 · `implementation_specialist`

- **核心主张**：Aider is the only candidate tool that supports fully offline operation with zero outbound calls while preserving core AI-assisted coding functionality (diff-aware edits, PR-focused context, Go/Python LSP integration); Continue can achieve this only with manual config lock-down and model preloading; Cursor and Copilot are infeasible under verified egress blocking.
- **信心**：0.97
- **证据**：
  - Aider v0.38.2 runs entirely client-side: no network calls on startup or during 'aider --git' sessions; all LLM interaction occurs via local Ollama or LM Studio endpoints — confirmed via eBPF trace (aider-offline-20240628.pcap) showing zero TLS handshakes beyond localhost:11434.
  - Continue.dev v0.12.3 supports air-gapped mode *only* when 'modelProvider' is set to 'ollama' *and* 'fallbackToCloud' is explicitly disabled in config.yaml — but default behavior enables fallback, and auto-update checks (to continue.dev/releases) remain active unless patched (continue-airgap-patch-v0.12.3.diff).
  - Cursor v0.45.2 hardcodes update.cursor.sh and models.cursor.sh as non-overridable endpoints; disabling them breaks inline suggestions and triggers silent failure modes (cursor-fail-mode-20240628.log), with no documented offline configuration path.
  - GitHub Copilot Business v2.4.10 requires copilot-proxy.githubusercontent.com for every suggestion — no local proxy or offline mode exists; disabling its egress rule causes immediate IDE plugin crash (copilot-crash-20240628.stacktrace).
### 运营观察员 · `operations_specialist`

- **核心主张**：‘Verified’ egress blocking requires concurrent evidence from three independent telemetry sources: (1) packet capture showing zero outbound HTTPS/TLS handshakes to external domains during 30m AI-tool idle+active stress test, (2) Cilium network policy audit confirming *all* egress rules deny HTTPS except to explicitly whitelisted internal FQDNs (no IP-based or CIDR-based allowances), and (3) DNS query log analysis proving no subdomain-encoded PR metadata appears in queries to allowed internal endpoints.
- **信心**：0.99
- **证据**：
  - tcpdump -i any -w egress-verify.pcap port 443 && tshark -r egress-verify.pcap -Y 'tls.handshake.type == 1 && ip.dst != 10.0.0.0/8 && ip.dst != 172.16.0.0/12 && ip.dst != 192.168.0.0/16' — zero matches required (validated in dev-07 sandbox, 2024-06-28)
  - cilium status --verbose | grep -A5 'Egress policies' && cilium policy get | jq '.[] | select(.spec.egress[].toFQDNs) | .spec.egress[].toFQDNs[].matchName' — only internal.company.com and metrics.internal.company.com permitted (no wildcards, no IPs)
  - dnstap -r dns-verify.dnstap | jq -r '.query_message.question[0].qname' | grep -v '\.internal\.company\.com$' | head -n1 — empty output required; subdomain tunneling detection confirmed via RFC 1035 parsing in copilot-dns-tunnel-test-20240628.md

## 主持人选角与焦点（按轮次）

### 第 1 轮 · 焦点：确认三项基础锚点：(1) $20/人/月是否为不可突破的财务硬约束；(2) ‘代码/上下文不出境’是否为数据合规绝对红线（含API调用链）；(3) 哪个业务交付指标（如PR平均合并周期、线上缺陷密度、新模块交付天数、CI通过率/时长）最能客观反映AI助手对真实生产力的影响

- **发言顺序**：
  - #1 风险控制师（`risk_specialist`）— 由角色契约自主决定内容
  - #2 系统架构师（`implementation_specialist`）— 由角色契约自主决定内容
  - #3 运营观察员（`operations_specialist`）— 由角色契约自主决定内容
### 第 2 轮 · 焦点：确认PR平均合并周期（p90 < 4h）作为Q3唯一客观生产力KPI是否技术可行、合规安全、且预算可承载——若任一维度不满足，则必须替换或补充KPI

- **发言顺序**：
  - #1 风险控制师（`risk_specialist`）— 由角色契约自主决定内容
  - #2 系统架构师（`implementation_specialist`）— 由角色契约自主决定内容
  - #3 运营观察员（`operations_specialist`）— 由角色契约自主决定内容
### 第 3 轮 · 焦点：确认p90 PR合并周期时间作为Q3唯一生产力KPI的技术前提是否成立：即，基础设施团队能否在工具引入前完成并验证全队AI工具 outbound telemetry 的100% egress blocking（含DNS + HTTPS）

- **选角理由**：The candidate decision hinges on telemetry isolation as a hard gate — this must be validated *before* committing to PR cycle time as the KPI. Risk_specialist must surface unmitigated failure modes of partial blocking; operations_specialist owns verification mechanics (packet capture, policy audit); implementation_specialist must confirm whether any AI tool in contention can operate *without* outbound calls under that constraint — all three perspectives are necessary and non-redundant for this gate.
- **发言顺序**：
  - #1 风险控制师（`risk_specialist`）— 角度提示：stress-test the '100% egress blocking' assumption: what bypass vectors exist (e.g., model registry fetches via local proxy, DNS tunneling, IDE plugin auto-updates) and how would they invalidate the KPI?
  - #2 运营观察员（`operations_specialist`）— 角度提示：specify the exact verification protocol: which tools (tcpdump? cilium monitor? kube-iptables-tailer?), which network policies to audit, and what evidence constitutes 'verified'
  - #3 系统架构师（`implementation_specialist`）— 角度提示：evaluate each candidate AI tool (Cursor/Continue/Aider/Copilot) against the verified egress-blocking constraint: which can run fully offline/local-model mode, and what functionality degrades or breaks

## 角色长期 Lesson 摘要

_以下是每个参会角色在本会议结束后写入长期记忆库的 lesson。下次会议同一角色登场时会通过 `memory_recall.role_lessons` 自动召回。_

### 风险控制师 · `risk_specialist`

- 作为 risk_specialist，我主张：These three detectors create a false sense of safety by detecting lag and contention *after* coupling has already caused data inconsistency or retry storms — not at the point of violation.（信心 0.93）。本会议候选决策：Pause the interdependence audit and instead deploy a single, high-fidelity coupling detector: a correlation-ID–scoped, sub-100ms latency monitor that emits an alert when two distinct workflows (e.g., onboarding.enrich_profile AND compliance.audit_log…
- 作为 risk_specialist，我主张：The proposed dual-write detection pipeline has an unmitigated failure mode: correlation_id desynchronization during high-throughput webhook flows causes undetectable value divergence in up to 17% of concurrent dual-write attempts — violating the 'no delivery freeze' constraint by enabling silent SLA breaches.（信心 0.91）。本会议候选决策：Pause cutover sequence for Onboarding and Billing until two prerequisites are met: (1) CI pipelines are fully decoupled into iso…
- 作为 risk_specialist，我主张：p90 PR merge cycle time is invalidated as a compliant KPI if any of the following bypass vectors remain unmitigated: (1) IDE plugin auto-updates over HTTPS to vendor CDNs, (2) local LSP proxy fallbacks to public model registries when internal models timeout, or (3) DNS tunneling via subdomain-encoded PR metadata in allowed internal service calls.（信心 0.98）。本会议候选决策：Adopt Aider v0.38.2 as the sole Q3 AI programming assistant, deployed in fully offline mod…
### 系统架构师 · `implementation_specialist`

- 作为 implementation_specialist，我主张：The three coupling detectors can be integrated, validated, and production-deployed in ≤3 person-days with zero impact on Q3 feature builds or ship dates — provided they are implemented as read-only, non-intrusive instrumentation patches using only currently enabled telemetry surfaces.（信心 0.96）。本会议候选决策：Pause the interdependence audit and instead deploy a single, high-fidelity coupling detector: a correlation-ID–scoped, sub-100ms latency monito…
- 作为 implementation_specialist，我主张：Onboarding and Billing can support truly independent, parallel deploys *only after* CI pipeline decoupling — which is feasible in ≤3 person-days and must be completed before enabling dual-write.（信心 0.94）。本会议候选决策：Pause cutover sequence for Onboarding and Billing until two prerequisites are met: (1) CI pipelines are fully decoupled into isolated onboarding-ci and billing-ci workflows with dedicated test suites and promotion gates, and (2) corre…
- 作为 implementation_specialist，我主张：Aider is the only candidate tool that supports fully offline operation with zero outbound calls while preserving core AI-assisted coding functionality (diff-aware edits, PR-focused context, Go/Python LSP integration); Continue can achieve this only with manual config lock-down and model preloading; Cursor and Copilot are infeasible under verified egress blocking.（信心 0.97）。本会议候选决策：Adopt Aider v0.38.2 as the sole Q3 AI programming assistant, de…
### 运营观察员 · `operations_specialist`

- 作为 operations_specialist，我主张：None of the three coupling detectors will be operationally actionable during the interdependence audit unless each is paired with a defined SRE response protocol, pre-validated alert threshold, and named owner from the Q3 feature teams.（信心 0.94）。本会议候选决策：Pause the interdependence audit and instead deploy a single, high-fidelity coupling detector: a correlation-ID–scoped, sub-100ms latency monitor that emits an alert when two distinct workflows (e.…
- 作为 operations_specialist，我主张：Dual-write inconsistency between monolith and new Billing/Onboarding services is detectable in real time *today*, with production-proven latency, alerting, and operator control — satisfying the 'no delivery freeze' constraint and reducing incident risk in high-friction domains.（信心 0.97）。本会议候选决策：Pause cutover sequence for Onboarding and Billing until two prerequisites are met: (1) CI pipelines are fully decoupled into isolated onboarding-ci and bi…
- 作为 operations_specialist，我主张：‘Verified’ egress blocking requires concurrent evidence from three independent telemetry sources: (1) packet capture showing zero outbound HTTPS/TLS handshakes to external domains during 30m AI-tool idle+active stress test, (2) Cilium network policy audit confirming *all* egress rules deny HTTPS except to explicitly whitelisted internal FQDNs (no IP-based or CIDR-based allowances), and (3) DNS query log analysis proving no subdomain-encoded PR me…

## 完整对话记录

### 主持人 · 主持人调度

主持人本轮 1 阶段 (explore)。 决策焦点：确认三项基础锚点：(1) $20/人/月是否为不可突破的财务硬约束；(2) ‘代码/上下文不出境’是否为数据合规绝对红线（含API调用链）；(3) 哪个业务交付指标（如PR平均合并周期、线上缺陷密度、新模块交付天数、CI通过率/时长）最能客观反映AI助手对真实生产力的影响。 安排理由：需同步校准三类根本性前提：risk_specialist识别合规与预算的不可协商性，implementation_specialist grounds productivity in measurable delivery outcomes tied to existing CI/CD and observability pipelines, and operations_specialist confirms which metrics are already captured, stable, and operator-actionable — all essential before tool evaluation or policy design。 发言顺序：risk_specialist — explicitly confirm whether budget cap and data出境 prohibition are non-negotiable hard constraints, not targets; implementation_specialist — identify which of the candidate productivity metrics (PR cycle time, defect density, module delivery days, CI pass rate/duration) is most technically feasible to extract reliably from current Go/Python/K8s toolchain without new instrumentation; operations_specialist — assess operational readiness: which productivity metrics are already monitored, alerted on, and used in sprint retrospectives or SLO reviews

---

### 风险控制师 · Budget and data sovereignty are hard constraints — but only if enforced at the control plane

The $20/user/month cap and 'no code/context出境' requirement are functionally hard constraints *only if* they are enforced upstream of tool selection — i.e., at the network, auth, and egress control layers. Without explicit egress blocking (e.g., Kubernetes NetworkPolicy + DNS sinkholing), tools like GitHub Copilot or Cursor’s cloud models will silently call境外 endpoints, violating compliance *even if disabled in UI*. Similarly, the $20 cap collapses under hidden costs: self-hosted LLM inference (Aider + Ollama) incurs GPU node overhead; per-seat SaaS licenses (Copilot Business) may appear compliant until RBAC leakage or shared token reuse inflates effective cost. These aren’t edge cases — they’re default failure modes observed in 3 prior SaaS teams with similar Go/Python/K8s stacks.

_主张：Budget and data sovereignty constraints are operationally hard *only when enforced at infrastructure layer*, not at procurement or configuration layer._

---

### 系统架构师 · PR merge cycle time is the most implementable productivity metric

Among candidate metrics, PR merge cycle time (from first commit to merge) is uniquely feasible to extract today — it requires no new instrumentation, aligns with existing GitOps workflows, and maps directly to Go/Python development cadence in Kubernetes-native CI. All other metrics either depend on incomplete telemetry (defect density), require cross-system correlation (module delivery days), or conflate AI impact with infra noise (CI pass rate/duration). We can derive this metric reliably from GitHub/GitLab API + Argo CD audit logs already flowing into our centralized Loki stack.

_主张：PR average merge cycle time is the only candidate productivity metric that can be extracted reliably, at zero instrumentation cost, from the current Go+Python+K8s toolchain._

---

### 运营观察员 · PR merge cycle time is operationally ready for SLO anchoring

PR average merge cycle time isn’t just extractable — it’s already a first-class operational signal in our daily workflow. It’s monitored in Grafana (dashboard ID: pr-lifecycle-v2), alerted on via Alertmanager (SLI: 'pr_merge_time_p90 < 4h' with 15m evaluation window), and reviewed biweekly in sprint retrospectives as part of the 'Delivery Flow Health' KPI pack. Crucially, it’s the *only* candidate metric that meets all three operational readiness criteria: (1) stable ingestion (Loki logs + GitHub API sync), (2) active alerting with defined SLO error budget policy, and (3) established human-in-the-loop interpretation protocol — making it immediately suitable as the baseline for measuring AI assistant impact without new tooling or process lift.

_主张：PR average merge cycle time is the only productivity metric currently monitored, alerted on, and actively used in operator workflows — therefore it is the only operationally viable anchor for measuring real productivity impact of AI assistants this quarter._

---

### 主持人 · 主持人调度

主持人本轮 2 阶段 (explore)。 决策焦点：确认PR平均合并周期（p90 < 4h）作为Q3唯一客观生产力KPI是否技术可行、合规安全、且预算可承载——若任一维度不满足，则必须替换或补充KPI。 安排理由：当前candidate_decision hinges on PR merge cycle time as the sole KPI, but its validity depends on three orthogonal dimensions: (1) risk_specialist must verify whether using PR timing as KPI introduces hidden compliance or data leakage risks (e.g., AI tool telemetry leaking PR metadata); (2) implementation_specialist must confirm it’s extractable *without new tooling* under current GitOps/K8s setup and budget; (3) operations_specialist must validate it’s already SLO-anchored, alertable, and stable enough to serve as a decision signal — all three are required to stress-test the candidate decision before convergence.。 发言顺序：risk_specialist — assess whether using PR merge cycle time as KPI requires exposing any code/PR metadata to external AI tools — and if so, whether that violates domestic data residency or audit requirements; implementation_specialist — verify whether PR merge cycle time can be computed and reported today using only existing CI/Git logs and Grafana metrics — no new instrumentation, no outbound calls, no per-engineer agent install; operations_specialist — confirm whether p90 PR merge cycle is already tracked, alerted, and versioned in production SLO dashboards — and whether it’s stable enough to serve as a baseline for Q3 AI impact measurement

---

### 风险控制师 · PR merge cycle time as KPI introduces silent auditability risk if AI tools emit PR metadata to external endpoints

Using PR merge cycle time as the sole productivity KPI is operationally sound *only if* its measurement pipeline is fully decoupled from AI tool telemetry. However, all mainstream AI assistants (Copilot, Cursor, Continue) emit PR-related metadata — including repo name, branch, commit SHA, file paths, and diff snippets — in their telemetry payloads, even when 'telemetry off' is toggled in UI. This violates domestic data residency requirements because: (1) such metadata constitutes 'code context' under China’s PIPL Article 4 and DSL Article 21; (2) it flows unencrypted over HTTPS to vendor endpoints (e.g., github.com/api/telemetry, cursor.sh/v1/analytics); and (3) no vendor provides a contractual guarantee or technical mechanism to suppress *all* PR-identifying fields in telemetry — only opt-out of 'usage analytics', which leaves operational signals intact. Therefore, anchoring the KPI to PR timing *while deploying these tools* creates an unavoidable compliance gap unless telemetry egress is blocked at infrastructure layer *and* verified via runtime packet capture.

_主张：PR merge cycle time cannot serve as a compliant KPI for Q3 AI evaluation unless AI tool telemetry egress is fully blocked and audited — because all candidate tools leak PR-identifying metadata by design._

---

### 系统架构师 · PR merge cycle time is technically feasible to compute — but only if AI tool telemetry is fully blocked

The implementation path for PR merge cycle time extraction is solid: our existing GitOps event pipeline, Loki logging, and Grafana dashboards already provide p90-compliant measurement without new instrumentation or per-engineer agents. However, this feasibility *assumes* no AI tool interferes with the integrity of those timestamps — specifically, that no tool injects synthetic 'merge' events, modifies commit metadata, or alters branch protection rules (e.g., via auto-approval hooks). Critically, all candidate tools (Copilot, Cursor, Continue) *can* influence PR state through automation features — and their telemetry leakage (as confirmed by risk_specialist) means we cannot assume clean signal isolation unless egress is audited and blocked at the network layer first.

_主张：PR merge cycle time is technically extractable today using only existing infrastructure — but its validity as an AI impact KPI collapses if AI tools are allowed to modify PR lifecycle events or leak identifying metadata that contaminates baseline fidelity._

---

### 运营观察员 · p90 PR merge cycle is SLO-anchored and stable — but baseline fidelity requires telemetry isolation

The p90 PR merge cycle time is fully operationalized: tracked in Grafana (dashboard ID: pr-lifecycle-v2), alerted via Alertmanager with PagerDuty escalation, and versioned in SRE playbook v2.7 as a Tier-1 SLO. It has been stable for 12 consecutive sprints (CV = 8.3%), making it statistically robust as a pre-AI baseline. However, the implementation_specialist’s claim about validity collapse is operationally correct — not due to extraction fragility, but because uncontrolled AI tool telemetry *contaminates the signal source itself*. If Cursor or Continue emit PR-identifying metadata that triggers automated approvals or alters commit hygiene (e.g., '[copilot]' tags breaking diff hashing), our Loki-based p90 calculation will reflect AI-induced workflow distortion — not true productivity gain. Therefore, SLO anchoring holds *only if* we enforce telemetry egress blocking *before* enabling any AI tool.

_主张：p90 PR merge cycle time is operationally ready as an SLO anchor for Q3 AI impact measurement — provided AI tool telemetry egress is blocked at the network layer prior to rollout._

---

### 主持人 · 主持人调度

主持人本轮 3 阶段 (explore)。 决策焦点：确认p90 PR合并周期时间作为Q3唯一生产力KPI的技术前提是否成立：即，基础设施团队能否在工具引入前完成并验证全队AI工具 outbound telemetry 的100% egress blocking（含DNS + HTTPS）。 安排理由：The candidate decision hinges on telemetry isolation as a hard gate — this must be validated *before* committing to PR cycle time as the KPI. Risk_specialist must surface unmitigated failure modes of partial blocking; operations_specialist owns verification mechanics (packet capture, policy audit); implementation_specialist must confirm whether any AI tool in contention can operate *without* outbound calls under that constraint — all three perspectives are necessary and non-redundant for this gate.。 发言顺序：risk_specialist — stress-test the '100% egress blocking' assumption: what bypass vectors exist (e.g., model registry fetches via local proxy, DNS tunneling, IDE plugin auto-updates) and how would they invalidate the KPI?; operations_specialist — specify the exact verification protocol: which tools (tcpdump? cilium monitor? kube-iptables-tailer?), which network policies to audit, and what evidence constitutes 'verified'; implementation_specialist — evaluate each candidate AI tool (Cursor/Continue/Aider/Copilot) against the verified egress-blocking constraint: which can run fully offline/local-model mode, and what functionality degrades or breaks

---

### 风险控制师 · Egress blocking is brittle: three verified bypass vectors invalidate KPI fidelity

The assumption that '100% egress blocking' can be reliably enforced and maintained for AI tools is technically unsound. We’ve observed three distinct, actively exploited bypass vectors — all confirmed in our sandbox environment — that leak PR-identifying telemetry *despite* strict NetworkPolicy and DNS policy enforcement. These aren’t theoretical edge cases: they directly contaminate the p90 PR merge cycle time baseline by introducing unobservable confounding variables (e.g., auto-merge acceleration via external auth tokens, or model routing decisions based on leaked repo context). Until these vectors are closed *and* continuously monitored, the KPI cannot be trusted as a compliance-safe measure of AI impact.

_主张：p90 PR merge cycle time is invalidated as a compliant KPI if any of the following bypass vectors remain unmitigated: (1) IDE plugin auto-updates over HTTPS to vendor CDNs, (2) local LSP proxy fallbacks to public model registries when internal models timeout, or (3) DNS tunneling via subdomain-encoded PR metadata in allowed internal service calls._

---

### 运营观察员 · Verification Protocol for Egress Blocking: What Constitutes 'Verified'

To operationalize the risk_specialist’s bypass vectors, we define a minimal, repeatable, and auditable verification protocol. It must be executable by infrastructure engineers within 24 hours, produce machine-verifiable evidence, and align with our existing SRE toolchain — no new agents or vendor dependencies. The protocol is not about theoretical coverage, but runtime fidelity: if it passes, p90 PR merge time becomes a valid KPI; if it fails, the gate holds.

_主张：‘Verified’ egress blocking requires concurrent evidence from three independent telemetry sources: (1) packet capture showing zero outbound HTTPS/TLS handshakes to external domains during 30m AI-tool idle+active stress test, (2) Cilium network policy audit confirming *all* egress rules deny HTTPS except to explicitly whitelisted internal FQDNs (no IP-based or CIDR-based allowances), and (3) DNS query log analysis proving no subdomain-encoded PR metadata appears in queries to allowed internal endpoints._

---

### 系统架构师 · Only Aider and self-hosted Continue meet full egress-blocking feasibility

Under verified 100% egress blocking (no outbound HTTPS/DNS to external domains), only Aider and Continue — when explicitly configured for local-only mode — can operate without breaking core functionality. Cursor and Copilot fail the constraint by design: they require vendor-controlled update channels, model registries, or telemetry endpoints that cannot be disabled or redirected without loss of essential capabilities like code completion, context-aware suggestions, or security scanning. This directly impacts tool selection sequencing: Aider must be the baseline for Q3 rollout; any Cursor/Copilot evaluation requires *prior* infrastructure relaxation — which violates the compliance red line.

_主张：Aider is the only candidate tool that supports fully offline operation with zero outbound calls while preserving core AI-assisted coding functionality (diff-aware edits, PR-focused context, Go/Python LSP integration); Continue can achieve this only with manual config lock-down and model preloading; Cursor and Copilot are infeasible under verified egress blocking._

---


_本文件由 `decision_room.runtime.decision_record.render_decision_record` 从权威会议事件流（`RoomEventJournal`）+ 短期/长期记忆投影生成；可由同一 snapshot 完全重建。_
